# scripts/demo-names.txt
# One learner name per line. Lines starting with # are ignored.
# After populating with 20 names, run:
#   node scripts/create-demo-trials.mjs --names scripts/demo-names.txt
# (add --live once you've set GOOGLE_APPLICATION_CREDENTIALS)
