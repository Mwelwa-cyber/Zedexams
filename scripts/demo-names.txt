# scripts/demo-names.txt
# One learner name per line. Lines starting with # are ignored.
#
# This file is a template — keep real learner names out of git.
# Either edit a local copy (e.g. scripts/demo-names.local.txt, which is
# gitignored) or pass --names <path> at the command line.
#
# Usage:
#   node scripts/create-demo-trials.mjs --names scripts/demo-names.local.txt
#   node scripts/create-demo-trials.mjs --names scripts/demo-names.local.txt --live
