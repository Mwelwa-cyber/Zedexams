{
  "_comment": "Phase 5 gate (b): every export whose frozen options the manifest follower CANNOT read. SHRINK-ONLY — a new name fails test:functions-manifest, and an entry that no longer applies must be DELETED from this file in the same PR. Generated once from the manifest; maintained by hand thereafter. See docs/phase5-plan.md.",
  "generatedFrom": "scripts/functions-manifest.json",
  "unresolved": {
    "agentJobsOnApproved": "factory-built: options are arguments, guarded by the factory's own tests",
    "agentJobsOnCreate": "factory-built: options are arguments, guarded by the factory's own tests",
    "aiLessonCount": "factory-built: options are arguments, guarded by the factory's own tests",
    "analyzeExamPaper": "factory-built: options are arguments, guarded by the factory's own tests",
    "analyzePaperLayout": "factory-built: options are arguments, guarded by the factory's own tests",
    "apiGenerateLessonPlan": "factory-built: options are arguments, guarded by the factory's own tests",
    "apiGenerateWorksheet": "factory-built: options are arguments, guarded by the factory's own tests",
    "checkVisualSafety": "factory-built: options are arguments, guarded by the factory's own tests",
    "deleteCurriculumUpload": "factory-built: options are arguments, guarded by the factory's own tests",
    "extractAssessmentFormat": "factory-built: options are arguments, guarded by the factory's own tests",
    "extractTopicsFromPdf": "factory-built: options are arguments, guarded by the factory's own tests",
    "generateAssessment": "factory-built: options are arguments, guarded by the factory's own tests",
    "generateDiagram": "factory-built: options are arguments, guarded by the factory's own tests",
    "generateFlashcards": "factory-built: options are arguments, guarded by the factory's own tests",
    "generateHomework": "factory-built: options are arguments, guarded by the factory's own tests",
    "generateLessonActivities": "factory-built: options are arguments, guarded by the factory's own tests",
    "generateLessonPlan": "factory-built: options are arguments, guarded by the factory's own tests",
    "generateNotePictures": "factory-built: options are arguments, guarded by the factory's own tests",
    "generateNotes": "factory-built: options are arguments, guarded by the factory's own tests",
    "generatePasskeyAuthenticationOptionsAfrica": "factory-built: options are arguments, guarded by the factory's own tests",
    "generatePasskeyRegistrationOptionsAfrica": "factory-built: options are arguments, guarded by the factory's own tests",
    "generateQuiz": "factory-built: options are arguments, guarded by the factory's own tests",
    "generateRubric": "factory-built: options are arguments, guarded by the factory's own tests",
    "generateSbaTask": "factory-built: options are arguments, guarded by the factory's own tests",
    "generateSchemeOfWork": "factory-built: options are arguments, guarded by the factory's own tests",
    "generateStudyPlan": "factory-built: options are arguments, guarded by the factory's own tests",
    "generateVisualNotes": "factory-built: options are arguments, guarded by the factory's own tests",
    "generateWorksheet": "factory-built: options are arguments, guarded by the factory's own tests",
    "getPlatformHealth": "factory-built: options are arguments, guarded by the factory's own tests",
    "importPastPaperQuestions": "factory-built: options are arguments, guarded by the factory's own tests",
    "initializeAgentPipeline": "factory-built: options are arguments, guarded by the factory's own tests",
    "lessonPlanTemplateOnWrite": "factory-built: options are arguments, guarded by the factory's own tests",
    "onAssessmentQuestionDeleted": "built by the local factory makeDeletedTrigger(…) in storageCleanup/onQuestionChange",
    "onAssessmentQuestionUpdated": "built by the local factory makeUpdatedTrigger(…) in storageCleanup/onQuestionChange",
    "onQuizQuestionDeleted": "built by the local factory makeDeletedTrigger(…) in storageCleanup/onQuestionChange",
    "onQuizQuestionUpdated": "built by the local factory makeUpdatedTrigger(…) in storageCleanup/onQuestionChange",
    "onUserDeleted": "v1 chained builder (functions.region().runWith()…) in storageCleanup/onUserDeleted",
    "planAssessment": "factory-built: options are arguments, guarded by the factory's own tests",
    "questionReviewOnWrite": "factory-built: options are arguments, guarded by the factory's own tests",
    "rebuildTableFromImage": "factory-built: options are arguments, guarded by the factory's own tests",
    "recordTemplateInteraction": "factory-built: options are arguments, guarded by the factory's own tests",
    "redrawTestPaperDiagram": "factory-built: options are arguments, guarded by the factory's own tests",
    "regenerateAssessmentQuestion": "factory-built: options are arguments, guarded by the factory's own tests",
    "resolvePaperAssetUrl": "factory-built: options are arguments, guarded by the factory's own tests",
    "reviseLessonSection": "factory-built: options are arguments, guarded by the factory's own tests",
    "reviseQuestion": "factory-built: options are arguments, guarded by the factory's own tests",
    "runSampleAgentJob": "factory-built: options are arguments, guarded by the factory's own tests",
    "sendTestFunctionErrorAlert": "factory-built: options are arguments, guarded by the factory's own tests",
    "sendTestOpsAlert": "factory-built: options are arguments, guarded by the factory's own tests",
    "studioGenerateLessonPlan": "factory-built: options are arguments, guarded by the factory's own tests",
    "suggestAnswer": "factory-built: options are arguments, guarded by the factory's own tests",
    "synthesizeAssessmentFormat": "factory-built: options are arguments, guarded by the factory's own tests",
    "uploadCurriculumModule": "factory-built: options are arguments, guarded by the factory's own tests",
    "verifyPasskeyAuthenticationAfrica": "factory-built: options are arguments, guarded by the factory's own tests",
    "verifyPasskeyRegistrationAfrica": "factory-built: options are arguments, guarded by the factory's own tests"
  }
}
