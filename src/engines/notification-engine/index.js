/**
 * Notification Engine — the client half of in-app notifications, FCM
 * registration and announcement display (docs/architecture.md §7, §12).
 *
 * Empty until Phase 4. Quiet hours are Lusaka time and dead-token pruning is
 * server-side; this engine renders and subscribes, it does not schedule.
 */

export {}
