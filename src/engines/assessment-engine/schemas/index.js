/**
 * Client-side zod schemas for the assessment contract, aligned with
 * `functions/shared/assessment/` rather than parallel to it.
 *
 * Empty until Phase 3. The existing `src/schemas/{quiz,attempt,result}.js`
 * remain authoritative until a consumer is switched, and the CJS/ESM
 * constant-mirror tests must stay green through the move (§4.1, §14.10).
 */

export {}
