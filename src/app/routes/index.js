/**
 * Route registry. Learner, admin and public routes join the data-driven
 * pattern `src/components/teacher/teacherRoutes.jsx` already uses, so
 * `scripts/lib/declaredRoutes.mjs` keeps seeing every declared route.
 *
 * Empty until Phase 4. Routes are declared in `src/App.jsx` and
 * `teacherRoutes.jsx` today; both remain authoritative until a route
 * physically moves here, and the route register (docs/architecture/
 * 03-route-register.md) is regenerated in the same PR when one does.
 */

export {}
