/**
 * Route layouts — TeacherLayout, AdminLayout, ParentLayout, LegalLayout.
 *
 * Empty until Phase 4. `TeacherLayout` is the one shell for the whole teacher
 * area and every `/teacher/*` route renders inside it; `teacherRoutes.spec.jsx`
 * fails if one does not. That test moves with the layout.
 */

export {}
