/**
 * Shared data contracts — zod schemas used across more than one feature.
 *
 * Empty until Phase 4. Assessment shapes are not shared-layer: they extend
 * `functions/shared/assessment/`, the one package the client and the server
 * both import, and their client-side zod form lives with the Assessment Engine
 * (§4.1, §14.10).
 */

export {}
