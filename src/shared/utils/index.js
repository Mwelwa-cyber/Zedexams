/**
 * Shared pure utilities — the residue of `src/utils/` that genuinely belongs to
 * no single feature (formatting, ids, dates, text).
 *
 * Empty until Phase 4. `src/utils/` is subdivided as each owning feature
 * migrates, so a file arrives here only after its callers show it has no owner
 * — moving one early just relocates the flat bucket.
 */

export {}
