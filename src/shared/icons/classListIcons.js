/**
 * The Lucide icons the Class List and Class Register surfaces use, named once.
 *
 * Imported by name from `lucide-react` rather than through the repo's
 * heroicons alias module (src/components/ui/icons.js) because these two
 * surfaces were specified against the Lucide set and several of the icons —
 * ClipboardPaste, ScanLine, RotateCcw — have no heroicons equivalent. Keeping
 * them in one file means the icon vocabulary of the register is a thing you
 * can read, and a swap is one edit rather than forty.
 *
 * No emoji. An emoji renders differently on every Android build a Zambian
 * school owns, and several of the ones a register would reach for do not
 * render at all on older WebViews.
 */

export {
  AlertTriangle,
  ArrowLeft,
  ArrowRight,
  Camera,
  CalendarDays,
  Check,
  CheckCircle2,
  ChevronDown,
  ChevronLeft,
  ChevronRight,
  ChevronUp,
  ClipboardPaste,
  Clock,
  CloudOff,
  Download,
  FileDown,
  FileSpreadsheet,
  Filter,
  GraduationCap,
  Info,
  Keyboard,
  Link2,
  ListFilter,
  Loader2,
  MoreVertical,
  Pencil,
  Plus,
  Printer,
  RefreshCw,
  RotateCcw,
  RotateCw,
  ScanLine,
  Search,
  Trash2,
  Undo2,
  Upload,
  UserMinus,
  UserPlus,
  Users,
  X,
} from 'lucide-react'
