/**
 * Shared presentation components — the ones with no feature knowledge
 * (buttons, dialogs, empty states, skeletons).
 *
 * Empty until Phase 4. A component that knows a curriculum rule, an AI prompt,
 * a Firestore query or payment logic is not shared — that is §14.6, and it is
 * the test for whether something belongs here at all.
 */

export {}
