/**
 * Shared hooks — the domain-free ones (media queries, debounce, clipboard,
 * intersection observers).
 *
 * Empty until Phase 4. A hook that reads a collection belongs to the feature
 * or service that owns that collection, not here.
 */

export {}
