# Firebase Functions environment — project: examsprepzambia
#
# Picked up automatically on deploy. NOT for secrets — use
# `firebase functions:secrets:set` for tokens, API keys, etc.
# This file is committed to git on purpose; only put non-sensitive config
# here.

# Zed Telegram assistant — founder allowlist.
# Username gate is the fallback; chat ID is the primary check (permanent).
ZED_TELEGRAM_ALLOWED_USERNAME=Mwelwam
ZED_TELEGRAM_ALLOWED_CHAT_ID=696206062
