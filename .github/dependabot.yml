# Dependabot — automated dependency + GitHub-Actions update PRs.
# Addresses production-readiness findings CICD-002 (dependency scanning) and
# CICD-007 (mutable action tags): Dependabot opens grouped update PRs weekly,
# and the `github-actions` ecosystem below keeps our third-party actions
# current (and, once any are SHA-pinned, bumps the pin deliberately).
#
# Scanning that *blocks* a merge on a newly-introduced critical prod vuln lives
# in ci.yml's `dependency-audit` job; Dependabot is the remediation half — it
# raises the PR that clears whatever the audit (or a new advisory) surfaces.
version: 2
updates:
  # Frontend / root package.json (the Vite SPA + all repo-level tooling).
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
    groups:
      # Split low-risk patch/minor bumps into small, logically-related groups
      # so each PR is reviewable on its own — a single all-in-one group produced
      # a 38-package batch (closed #1870) that couldn't be reviewed or bisected.
      # A dependency lands in the FIRST group whose patterns match; the trailing
      # misc-minor-and-patch catch-all keeps everything else from splintering
      # into dozens of single-package PRs. Majors are never grouped here — they
      # come as their own PRs to be assessed individually.
      react:
        applies-to: version-updates
        patterns:
          - "react"
          - "react-dom"
          - "react-router"
          - "react-router-dom"
          - "scheduler"
          - "@types/react"
          - "@types/react-dom"
        update-types:
          - "minor"
          - "patch"
      firebase-sdk:
        applies-to: version-updates
        patterns:
          - "firebase"
          - "@firebase/*"
        update-types:
          - "minor"
          - "patch"
      build-tooling:
        applies-to: version-updates
        patterns:
          - "vite"
          - "vite-plugin-*"
          - "@vitejs/*"
          - "rollup"
          - "@rollup/*"
          - "rolldown"
          - "esbuild"
          - "@esbuild/*"
          - "postcss"
          - "autoprefixer"
          - "tailwindcss"
          - "@tailwindcss/*"
          - "workbox-*"
        update-types:
          - "minor"
          - "patch"
      testing:
        applies-to: version-updates
        patterns:
          - "vitest"
          - "@vitest/*"
          - "@testing-library/*"
          - "jsdom"
          - "happy-dom"
        update-types:
          - "minor"
          - "patch"
      linting:
        applies-to: version-updates
        patterns:
          - "eslint"
          - "@eslint/*"
          - "eslint-plugin-*"
          - "eslint-config-*"
          - "globals"
          - "lint-staged"
          - "husky"
          - "prettier"
        update-types:
          - "minor"
          - "patch"
      editor:
        applies-to: version-updates
        patterns:
          - "@tiptap/*"
          - "prosemirror-*"
          - "katex"
          - "marked"
          - "dompurify"
        update-types:
          - "minor"
          - "patch"
      i18n:
        applies-to: version-updates
        patterns:
          - "i18next"
          - "i18next-*"
          - "react-i18next"
        update-types:
          - "minor"
          - "patch"
      observability:
        applies-to: version-updates
        patterns:
          - "@sentry/*"
          - "@sentry-internal/*"
          - "posthog-js"
        update-types:
          - "minor"
          - "patch"
      misc-minor-and-patch:
        applies-to: version-updates
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"
    ignore:
      # Firebase SDK majors are coordinated, breaking migrations (auth
      # persistence, App Check, Firestore cache) — never take them automatically.
      - dependency-name: "firebase"
        update-types: ["version-update:semver-major"]
      # puppeteer is NOT an ordinary dev dependency here: it supplies the
      # Chromium that renders the visual-regression baselines. `renderEnvironment.js`
      # resolves the binary through `puppeteer.executablePath()` and stamps the
      # version into each baseline's identity (`browser-print/chromium-<version>`),
      # so ANY puppeteer bump changes that identity and makes every browser-print
      # baseline incomparable — `assertComparableEnvironment` then fails the
      # REQUIRED `Visual regression gate` check. #2255 is what that looks like:
      # nine fixtures reported as infrastructure failures over
      # `chromium: 151.0.7922.47 → 151.0.7922.71`, with nothing wrong in the diff.
      #
      # Upgrading the renderer is therefore a deliberate, paired act, never an
      # automatic weekly bump: bump puppeteer and re-record the baselines through
      # `Visual baseline update` (workflow_dispatch, default branch only) in the
      # same change, so the gate is comparing like with like.
      #
      # The matching half of this is the EXACT pin in package.json — a caret here
      # lets any lockfile regeneration drift Chromium silently, which is the same
      # failure without a pull request to notice it in.
      - dependency-name: "puppeteer"
        update-types:
          - "version-update:semver-major"
          - "version-update:semver-minor"
          - "version-update:semver-patch"

  # Cloud Functions (functions/ has its own package.json + lockfile).
  - package-ecosystem: "npm"
    directory: "/functions"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "functions"
    groups:
      functions-minor-and-patch:
        applies-to: version-updates
        update-types:
          - "minor"
          - "patch"
    ignore:
      # firebase-functions / firebase-admin majors change the runtime contract
      # (v2 triggers, region pinning) — take them deliberately, not automatically.
      - dependency-name: "firebase-functions"
        update-types: ["version-update:semver-major"]
      - dependency-name: "firebase-admin"
        update-types: ["version-update:semver-major"]

  # GitHub Actions used across .github/workflows/ (CICD-007). Keeps third-party
  # actions (android-actions/setup-android, r0adkll/upload-google-play,
  # anthropics/*) current and surfaces upstream changes as reviewable PRs.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
    open-pull-requests-limit: 5
    labels:
      - "ci"
      - "github-actions"
